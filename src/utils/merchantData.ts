
// Remove or comment out mock data functions related to stamp cards
// This file can be cleaned up or repurposed as needed

export const getMerchantStampCard = () => {
  console.warn("Mock data retrieval is no longer supported. Use Supabase directly.");
  return null;
};

export const createMerchantStampCard = async () => {
  console.warn("Mock data creation is no longer supported. Use Supabase directly.");
};

export const updateMerchantStampCard = async () => {
  console.warn("Mock data update is no longer supported. Use Supabase directly.");
};
